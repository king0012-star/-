@@ -16,10 +16,12 @@ const插件= [
    exclude：'node_modules / **' ，
    //仅包含必需的助手
    externalHelpersWhitelist：[
      'defineProperties' ，
      'createClass' ，
      'inheritsLoose' ，
      'createSuper' ，
      'defineProperties' ，
      'defineProperty' ，
      'getPrototypeOf' ，
      'inheritsLoose' ，
      'objectSpread2'
    ]
  } ）
